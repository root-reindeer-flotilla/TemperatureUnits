{
  "version": 1,
  "settings": {
    "boolean": {
      "USE_FAHRENHEIT": {
        "name": "use-fahrenheit",
        "ingui": "temperatureunits:setting-use-fahrenheit",
        "weight": 1.0,
        "comment": "When enabled, all in-game temperatures will be displayed in Fahrenheit instead of Celsius. Uses the NIST-standard conversion formula.",
        "default": true,
        "clientSide": true
      }
    },
    "string": {
      "DECIMAL_PLACES": {
        "name": "decimal-places",
        "ingui": "temperatureunits:setting-decimal-places",
        "weight": 2.0,
        "comment": "Number of decimal places to display for converted temperatures.",
        "default": "1",
        "values": ["0", "1", "2"],
        "clientSide": true
      }
    }
  }
}
